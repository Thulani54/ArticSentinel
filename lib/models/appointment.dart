class Appointment {
  int id;
  String firstName;
  String lastName;
  String topic;
  String description;
  DateTime appointDate;

  Appointment(this.id, this.firstName, this.lastName, this.topic,
      this.description, this.appointDate);
}
